[package]
name = "openvm-stark-sdk"
version.workspace = true
authors.workspace = true
edition.workspace = true
rust-version.workspace = true
homepage.workspace = true
repository.workspace = true
license.workspace = true
description = "SDK for production and testing usage of STARKs."

[dependencies]
blake3 = { version = "1", default-features = false, optional = true }
openvm-stark-backend = { workspace = true }
openvm-cpu-backend = { workspace = true, optional = true }

p3-baby-bear = { workspace = true }
p3-field = { workspace = true }
p3-poseidon2 = { workspace = true, optional = true }
p3-bn254 = { workspace = true, optional = true }
zkhash = { workspace = true, optional = true }

hex-literal.workspace = true
itertools.workspace = true
tracing.workspace = true
serde = { workspace = true, features = ["alloc"] }
serde_json.workspace = true
rand.workspace = true
metrics = { workspace = true, optional = true }
dashmap.workspace = true
tracing-subscriber = { workspace = true, features = ["std", "env-filter"] }
tracing-forest = { workspace = true, features = ["ansi", "smallvec"] }
metrics-tracing-context.workspace = true
metrics-util.workspace = true
metrics-exporter-prometheus = { workspace = true, features = [
  "push-gateway",
], optional = true }
tokio = { workspace = true, features = [
  "rt",
  "rt-multi-thread",
], optional = true }
rustls = { workspace = true, features = ["aws-lc-rs"], optional = true }
eyre.workspace = true
static_assertions.workspace = true
derive-new.workspace = true
num-bigint = { workspace = true, optional = true }
nvtx = { workspace = true, optional = true }

[dev-dependencies]
p3-keccak-air.workspace = true
p3-poseidon2.workspace = true
zkhash.workspace = true
cfg-if.workspace = true
sha3.workspace = true # already in tree via zkhash

[features]
# ON BY DEFAULT IN THIS FORK so the backend's OWN tests exercise Blake3F — a 2-second loop
# instead of the 500-second prove-and-verify cycle I spent hours on.
blake3-hash = ["dep:blake3"]
default = ["parallel", "metrics", "cpu-backend"]
baby-bear-bn254-poseidon2 = [
  "dep:p3-bn254",
  "dep:zkhash",
  "dep:p3-poseidon2",
  "dep:num-bigint",
  "openvm-stark-backend/multi-field-transcript",
]
cpu-backend = ["dep:openvm-cpu-backend"]
parallel = ["openvm-stark-backend/parallel", "openvm-cpu-backend?/parallel"]
mimalloc = ["openvm-stark-backend/mimalloc"]
jemalloc = ["openvm-stark-backend/jemalloc"]
jemalloc-prof = ["openvm-stark-backend/jemalloc-prof"]
metrics = ["dep:metrics", "openvm-stark-backend/metrics", "openvm-cpu-backend?/metrics"]
prometheus = [
  "metrics",
  "dep:metrics-exporter-prometheus",
  "dep:tokio",
  "dep:rustls",
]
nvtx = ["dep:nvtx"]
